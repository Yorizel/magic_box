import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
  searchBarContainer: {
    width: '80%',
    maxWidth: 300,
  },
  avatarContainer: {
    textTransform: 'none',
    borderRadius: 25,
  },
}))
